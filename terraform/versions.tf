# Pin Terraform itself +  the providers we use.
# Displays if the version does not match

terraform {
  required_version = ">= 1.5"

  required_providers {
    docker = {
      source  = "kreuzwerker/docker"
      version = "~> 3.0"
    }
  }
}
